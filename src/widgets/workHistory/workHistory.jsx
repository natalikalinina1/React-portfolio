import React from "react";
import { Element } from "react-scroll";

import "./workHistory.css";

export const WorkHistory = ({name}) => {
    return <Element name={name} className="work">Work History</Element>
}